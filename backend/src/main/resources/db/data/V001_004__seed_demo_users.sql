-- PIA Tracker — V001_004: intentionally empty.
--
-- The former demo-user seed (EMP001-006) was removed. System administrators are now
-- seeded in V085_001__seed_system_users.sql instead — it MUST run after V085, which
-- adds the password_hash column that the admin seed populates. Seeding here would fail
-- with "column password_hash does not exist" because V001_004 runs long before V085.
--
-- All other officers are provisioned via scripts/import_users.py. This file is kept as
-- a no-op to preserve the Flyway version sequence and document the change.
